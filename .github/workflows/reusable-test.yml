name: Reusable Test

on:
  workflow_call:
    inputs:
      coverage-threshold:
        type: number
        default: 80

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: api/package-lock.json

      - run: npm ci
        working-directory: api

      - run: npm run test:cov -- --coverageThreshold='{"global":{"lines":${{
          inputs.coverage-threshold }}}}'
        working-directory: api

      - uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: api/coverage/

# name: Reusable Test

# on:
#   workflow_call:
#     inputs:
#       coverage-threshold:
#         type: number
#         required: false
#         default: 80

# jobs:
#   test:
#     name: Unit Tests & Coverage
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: npm
#           cache-dependency-path: api/package-lock.json

#       - name: Install dependencies
#         run: npm ci
#         working-directory: api

#       - name: Run tests with coverage
#         run: npm run test:cov -- --coverageThreshold='{"global":{"lines":${{ inputs.coverage-threshold }}}}'
#         working-directory: api

#       - name: Upload coverage report
#         uses: actions/upload-artifact@v4
#         with:
#           name: coverage-report
#           path: api/coverage/
#           retention-days: 7
